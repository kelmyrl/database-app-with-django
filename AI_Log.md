## Connect to PostgreSQL using a DATABASE_URL

Prompt:

Help me configure this Django project to connect to PostgreSQL using a DATABASE_URL environment variable stored as a GitHub Codespaces secret. Identify any needed Python packages, show the install command, update requirements.txt, and update settings.py without hard-coding the database URL or password in any repository file.

Result: 

Configured Django to use "DATABASE_URL" from environment variables so no database credentials are hard-coded in the repository. PostgreSQL connection parsing is now handled through "dj-database-url."

My changes:

- Added required database packages to "requirements.txt"
- Updated "hello_world/settings.py" to
- Added Codespaces-friendly host/origin handling driven by environment variables.

Verification:

- Install dependencies: "pip install -r requirements.txt"
- Confirm env var exists in Codespaces
- Run DB checks/migrations
- Start the app: "python manage.py runserver"
- Open the app home page and confirm no database connection errors in server logs.

## Generate Models fromt eh Existing Database

Prompt:

Explain these Django models generated by inspectdb. Which models represent existing database tables, and why did Django mark them as unmanaged?

Result: 

Explained that these models came from "inspectdb" and map to existing tables in the connected PostgreSQL database. Clarified that Django marks them with "managed = False" because the tables already exist and should not be created/altered/dropped by Django migrations.

My changes:

- Kept existing introspected models in "mythical_app/models.py" for tables such as "patient", "owner", "visit", "invoice", etc.
- Preserved "Meta.managed = False" and "db_table = '...'" on database-owned tables.
- Left field and table names aligned with existing schema generated by "inspectdb".

Verification:

- Run: "python manage.py makemigrations --check"
- Open "mythical_app/models.py" and verify existing DB tables still have:
	- "managed = False"
	- matching "db_table" names.

## Enable the Django Admin Interface

Prompt:

Register these Mythical Mane models with the Django admin. Add useful list_display, search_fields, list_filter, and date_hierarchy settings where appropriate. Keep the configuration simple and explain any assumptions.

Result: 

Registered the Mythical Mane models in Django admin so records can be browsed and managed through "/admin/".

My changes:

- Updated "mythical_app/admin.py".
- Registered all current models.
- Used straightforward "admin.site.register(...)" registration for each model.

Verification:
- Create a superuser if needed: "python manage.py createsuperuser"
- Run server: "python manage.py runserver"
- Open "/admin/" and log in.
- Verify each registered model appears in the admin sidebar and opens list/detail pages.

## Build a Patient List Page

Prompt:

Create a Django patient list view for the mythical_mane app. Query Patient objects with their related Owner and Universe data efficiently, add a URL pattern for /patients/, and create a Tailwind-styled template that displays the patient name, color, date of birth, owner name, and universe name.

Result: 

Built a patient list feature that serves "/patients/", fetches related owner/universe data efficiently, and renders a Tailwind-styled table with the required fields.

My changes:

- Added patient list view in "mythical_app/views.py":
- Added route in "mythical_app/urls.py":
	- "path("patients/", views.patient_list, name="patient_list")"
- Included app URLs in project routing ("hello_world/urls.py") so "/patients/" resolves.
- Created template "mythical_app/templates/mythical_app/patient_list.html" with Tailwind styling and columns for:
	- patient name
	- color
	- date of birth
	- owner name
	- universe name

Verification:

- Run server: "python manage.py runserver"
- Open "/patients/" in the browser.
- Confirm page loads and table shows patient name, color, DOB, owner, and universe.
- Confirm empty-state row appears if no patient data exists.
- Optional performance check: Django Debug Toolbar or query logging should show related objects are fetched via joined query behavior.

## Create a Django-Owned Model and Migrate It

Prompt:
Create at least one new model in Django and use migrations to add it to the Supabase database. Suggested model: CareNote Recommended fields:
Patient foreign key
Note text
Created timestamp
Follow-up date, optional
Resolved flag
Use Django migrations to create the new table.

Result: 

Created a new Django-managed model "CareNote" and added it through migrations so Django owns this new table while leaving existing introspected tables unmanaged.

My changes:

- Added "CareNote" model in "mythical_app/models.py" with requested fields.
- Set "db_table = 'care_note'" in model "Meta".
- Applied migrations ("python manage.py migrate").

Verification:

- Show migration state: "python manage.py showmigrations mythical_app"
- Apply migrations: "python manage.py migrate"
- Open "/admin/" and confirm "CareNote" appears and records are manageable.
- Re-open "/patients/" to ensure app still runs normally after schema changes.